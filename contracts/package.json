{
  "name": "ns-demographics-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NS Demographics",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts"
  },
  "dependencies": {
    "@semaphore-protocol/contracts": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  },
  "private": true
}

